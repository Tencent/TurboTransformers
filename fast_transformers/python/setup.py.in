from setuptools import setup, Extension

with open("stub.c", "w") as f:
    f.write("\n")


setup(name='fast-transformers',
      version='@FAST_TRANSFORMERS_VERSION@',
      description='Fast transformers',
      packages=[
          'fast_transformers',
          'fast_transformers.layers'
      ],
      package_data={
          '': [
              '*.so',
              '*.pyi'
          ]
      },
      ext_modules=[Extension('_fast_transformers_stub_ext', ['stub.c'])],
      include_package_data=True,
      install_requires=[
        "torch",
        "transformers"
      ]
      )
