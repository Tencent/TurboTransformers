from setuptools import setup, Extension

with open("stub.c", "w") as f:
    f.write("\n")


setup(name='easy-transformers',
      version='@easy_transformers_VERSION@',
      description='easy_transformers',
      packages=[
          'easy_transformers',
          'easy_transformers.layers'
      ],
      package_data={
          '': [
              '*.so',
              '*.pyi'
          ]
      },
      ext_modules=[Extension('_easy_transformers_stub_ext', ['stub.c'])],
      include_package_data=True,
      install_requires=[
        "torch",
        "transformers"
      ]
      )
